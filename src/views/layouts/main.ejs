<!doctype html>
<html lang="ru">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= title || 'Детская зимняя одежда' %></title>
  <link rel="stylesheet" href="/css/style.css?v=auth-mobile-1">
</head>
<body>
  <%- include('../partials/header') %>
  <main class="page <%= path === '/' ? 'home-page' : '' %> <%= path === '/catalog' ? 'catalog-page' : '' %> <%= path === '/cart' ? 'cart-page-main' : '' %>">
    <%- include('../partials/flash') %>
    <%- body %>
  </main>
  <%- include('../partials/footer') %>
  <script src="/js/main.js?v=auth-login-1"></script>
</body>
</html>
